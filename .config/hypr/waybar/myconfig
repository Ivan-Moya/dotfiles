{
    // General Settings
    "height":"16",
    "layer": "top",
    "margin-bottom": 0,
    "margin-left": 0,
    "margin-right": 0,
    "spacing": 2,  // Unified spacing value
    "include": ["~/.config/hypr/waybar/modules.json"],  // Full path for safety

    // Modules Left
    "modules-left": [
        "custom/appmenu",
        "hyprland/window",
        "custom/keyboard",
        "custom/updates",
        "custom/pacman",
        //"wlr/taskbar",
        "tray"
    ],

    // Modules Center
    "modules-center": [
        "hyprland/workspaces",
        "mpd"
    ],

    // Modules Right
    "modules-right": [
        "group/sys-info",
        "group/clock",
        "backlight",  // Fixed from "/backlight"
        "custom/power-menu"
    ]
}
